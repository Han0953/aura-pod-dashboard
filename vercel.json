{
  "ignoreCommand": "git log -1 --pretty=%B | grep -qiE '\\[skip ci\\]|\\[ci skip\\]|\\[skip vercel\\]|\\[vercel skip\\]' && exit 0 || exit 1",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
